/**
 * Re-export centralized emoji types
 */
export type {
  EmojiItem,
  EmojiCategoryData,
  CategoryTab,
  CustomEmoji,
  EmojiReactionData,
} from '../../types/emoji';

export { DEFAULT_CATEGORY_TABS } from '../../types/emoji';